import { createStyles } from "@material-ui/core/styles";

export const styles = createStyles({
  root: {
    width: "90%",
    overflowX: "auto"
  },
  table: {
    minWidth: 650
  },
  head: {
    fontWeight: "bold"
  },
  body: {
    textAlign: "center"
  },
  searchInput: {
    width: 100
  }
});

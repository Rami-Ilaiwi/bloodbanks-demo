import { createStyles, Theme, lighten } from "@material-ui/core/styles";

export const styles = createStyles({
  root: {
    width: "70%",
    overflowX: "auto",
    margin: "2%"
  },
  table: {
    minWidth: 650
  },
  head: {
    fontWeight: "bold"
  },
  body: {
    textAlign: "center"
  }
});
